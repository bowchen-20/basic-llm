# Basic LLM from Scratch

A minimal language model built from scratch in Python using only NumPy and PyTorch.

## Roadmap

- [x] Project setup
- [ ] Tokenizer (character-level BPE)
- [ ] Bigram language model
- [ ] Transformer (attention + feedforward)
- [ ] Training loop
- [ ] Text generation

## Usage

```bash
pip install -r requirements.txt
python train.py
```
